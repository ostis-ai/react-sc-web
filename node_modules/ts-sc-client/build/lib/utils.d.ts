import { ScAddr } from "./ScAddr";
import { ScConstruction } from "./ScConstruction";
import { SnakeToCamelCase } from "./types";
export declare const transformConnectorInfo: (construction: ScConstruction, aliasOrAddr: ScAddr | string) => void | {
    type: string;
    value: number;
};
export declare const shiftMap: (map: Map<any, any>, to?: number) => void;
export declare const snakeToCamelCase: <Str extends string>(str: Str) => SnakeToCamelCase<Str>;
