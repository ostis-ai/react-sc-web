import { ReactNode } from 'react';
interface IProps {
    disabled?: boolean;
    label: string;
    children: ReactNode;
}
export declare const OptionGroup: ({ label, disabled, children }: IProps) => import("react/jsx-runtime").JSX.Element;
export {};
